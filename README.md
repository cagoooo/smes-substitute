# 📅 石門國小線上調代課系統

桃園市龍潭區石門國民小學的全校課表與調代課管理系統。原創為[新北市中和高中教學組 詩穎老師 線上調代課系統](https://docs.google.com/spreadsheets/d/15xY1bGLeNaMg_t4GOjeDNje_R2goZI5uMKVMHnofFOA/copy)公開模板，由阿凱老師改良調整為國小情境。

🌐 **線上系統**：https://cagoooo.github.io/smes-substitute/

## 架構

```
老師瀏覽器 ──> GitHub Pages（docs/，純靜態前端 + Google 登入）
                │  fetch + id_token
                ▼
             Google Apps Script /exec（gas/，JSON API）
                │
                ▼
             Google 試算表（排課資料庫 / 紀錄表 / 名單）
```

- **前端**（`docs/index.html`）：GitHub Pages 靜態頁，Google Identity Services 登入，老師不需授權任何敏感權限
- **後端**（`gas/`）：Apps Script 以 `doPost` 提供 JSON API，驗證 id_token 後依「Email對照表」名單控管身分；透過 clasp 推送維護
- **試算表**：課表資料、調代課單據、教師名單的單一資料來源；出單（A5 三聯單）與兼代課鐘點結算由試算表選單執行

## 主要功能

- 教師視角週課表，點選節次即可媒合「代課」（同班/同科智慧排序）或「調課」（雙方空堂交叉比對）
- 提交後自動寄邀請信給受邀教師，一鍵同意/拒絕，狀態機控管（待確認 → 可出單 → 已出單）
- 管理員（教學組）可代任何教師直接建單，並以 Google Chat webhook 即時接收通知
- 代課／調課通知單 A5 三聯列印（教師聯、班級聯、留存聯，自動合併連續節次）
- 💰 兼代課鐘點結算：輸入月份自動產出「代課教師應領總表＋自費代課對帳表＋逐筆明細」

## 部署與維護

後端更新（保留同一 `/exec` 網址）：

```bash
cd gas
clasp push -f
clasp create-version "更新說明"
clasp update-deployment <deploymentId> -V <版本號> -d "更新說明"
```

前端更新：改 `docs/index.html` 後 push main 即自動部署。

## 📅 更新日誌與開發進度表

- **`[x]` (2026.07.08-3)**：**P1-6 教師個人頁本月彙整**。「調代課異動摘要」面板上方新增本月小結：本月**代課節數**、**應領鐘點費預估**（節數×該類別費率）、**調課次數**。老師登入自己的頁面即可看到當月實績（含未代=0 也顯示），不必等教學組月結。純前端計算（`FULL_CHANGES` 本月統計＋`FEE_CATEGORIES` 費率），管理員切換查詢教師時也會顯示該師的彙整。（P1-5 本土語代課專用登記依實際頻率低，決定維持人工，跳過。）
- **`[x]` (2026.07.08-2)**：**P1-4 代課媒合智慧排序升級（公平輪替＋避開滿堂）**。原本只有同班/同科加分，現改為：同班＋3、同科＋2，再依「本月已被派代課次數」扣分（公平輪替，避免同一人一直被派）、「當天自己授課節數」扣分（避開滿堂）綜合排序；每位候選顯示「本月已代 N・當天 N 節」與「本月未代」徽章。純前端計算（用 `FULL_CHANGES.allSub` 本月統計），不增後端負擔。
- **`[x]` (2026.07.08-1)**：**P1-3 課表匯入前新舊差異比對**。匯入預覽時比對「新解析 vs 目前線上教師課表」，列出🆕新增老師／➖移除老師／✏️課表有異動（每位顯示 ＋新增節 －移除節 ～更換課程班級）／✅未變動數，組長確認差異後才匯入，避免手滑覆蓋。後端 `diffScheduleSheets_`；`previewSchedule` API 一併回傳 diff。另修正 `bump-version.ps1` 改用 `git rev-parse` 錨定 repo 根目錄＋UTF8 讀取 version.json（免 PowerShell cwd 跑到 gas 導致相對路徑/`git add` 掃錯）。
- **`[x]` (2026.07.07-7)**：修正 SW 更新橫幅重複出現、第二次按無反應（SW 事件與 version.json 輪詢雙偵測源打架）：更新按鈕改在「點擊當下」才查 `reg.waiting` 做 skip-waiting（統一 `applyUpdate()`），一次點擊乾淨完成、不再雙重橫幅。
- **`[x]` (2026.07.07-6)**：**P1-2 課表匯入器支援本土語教師課表**。上傳時自動辨識頁面類型：班級課表照舊重建；「本土語教師課表 PDF」（台語/客語教師視角）自動解析並**合併進教師課表**（空堂才填、與既有課衝突則略過）。教師課表由 61 → **66 位**（新增台語/客語 5 位＋鍾杏宜合併）。後端 `buildScheduleSheets_(classes, supplement)` 新增補充教師 merge。科任/行政教師視角 PDF 會被自動過濾（其課本就已在班級課表中）。族語/新住民語/資源班因屬外聘/直播/抽離特教且表格不規則，維持人工處理。
- **`[x]` (2026.07.07-5)**：🎯 **P1-1 前端一鍵上傳課表匯入器**。管理控制台新增「📥 課表匯入」分頁：管理員直接上傳各年級「班級課表」PDF（可多選），前端以 pdf.js 解析（與後端同款：科目/老師/教室字體切分、座標還原格線），呼叫 `previewSchedule` 顯示班級數/筆數/教師數/排課衝突預覽，確認後呼叫 `importSchedule`（先自動備份舊表再重建＋清快取）。**教學組長／資訊組長每學期可自行更新全校課表，不需工程介入**。後端新增 `importSchedule` / `previewSchedule` 兩支管理員專用 API。
- **`[x]` (2026.07.07-4)**：靜默更新後補跳「已更新」提示。保留「登入且閒置時靜默換版不打斷」邏輯，但於換版前用 `sessionStorage` 設旗標，讓重整後的新頁面補跳一個 2 秒非阻斷 toast「✅ 已更新到最新版 vX」再清旗標（不重複跳），解決「更新無感、不知道換版了沒」。
- **`[x]` (2026.07.07-3)**：🎯 **全校真實課表匯入機制 + 管理員模擬教師登入**：
  - 新增 `gas/ScheduleImport.js`：以 pdf.js 解析排課系統匯出的 6 份年級「班級視角」課表 PDF（科目/老師/教室以字體大小切分、座標還原格線），反轉重建「排課資料庫（班級視角）」與「教師課表（教師視角 122 欄）」兩張互相一致的表；寫入前自動把舊表備份成「_備份_日期時間」分頁、**寫入後自動清快取**。114 上學期實匯 **32 班 / 排課資料庫 904 筆 / 教師課表 61 位 / 排課衝突 0**。本土語文為全年級跨班分組，掛「本土語教師」代課走人工。
  - 後端新增 `initAs(email)`（`checkAdminOrStaff_` 守門、抑制通知），前端管理控制台名單每列加「🎭 模擬」鈕，一鍵以任一教師身分重繪登入畫面＋頂部退出橫幅（`initSystem` 改為可重入），供管理員快速驗證各教師帳號的課表與權限是否正確，並可一眼抓出「Email對照表 vs 教師課表」姓名不一致者。
  - 修正匯入後未清快取導致「查得到老師名字卻課表空白」（`getAllInitData` 快取 2 小時）；清快取改由 clasp 全自動端點觸發，免管理員手動。
- **`[x]` (2026.07.07-2)**：實作全新 P0 與 P1 管理控制台：教師 Email 授權名單線上增修刪、兼代課鐘點月結算分頁、管理員一鍵模擬教師登入畫面等優化項目。
- **`[x]` (2026.07.06-3)**：修復自動升版腳本的 Git 提交中文亂碼問題與更新提示優化：
  - 調整 `bump-version.ps1` 內部的中文字常數改為 Unicode 碼拼裝，避開 PowerShell 5.1 解析 UTF-8 腳本的亂碼 Bug。
  - 將提交訊息以 `.NET File API` 寫入標準 UTF-8 檔案並配合 `git commit -F` 提交，徹底避免 Windows 控制台引數傳遞的字元集轉換。
  - 支援 `-notes` 命令列參數，免去互動式 `Read-Host` 的 Stdin 編碼錯位，且在 `.gitignore` 排除暫存檔。
  - 依使用者要求，微調 `isSafeToSilentUpdate()` 邏輯，當處於未登入頁面時也照常彈出「✦ 系統有新版本」的通知橫幅。
- **`[x]` (2026.07.06-8)**：實作新一輪 P0 安全防護與快取性能優化：
  - 優化 Google Chat Webhook 推播 JSON Payload，最外層加入自動化預覽文字（fallbackText），解決手機推播通知只顯示「傳送了一個附件」的經典痛點。
  - 在後端 Apps Script 中導入 `CacheService` 記憶體快取，快取 Email授權對照表、靜態教師課表、教學節數及彈性課程資料（快取 2 小時），大幅提升前端載入與課表切換速度。
  - 於試算表自訂選單新增「🧹 清除系統快取 (課表變更時執行)」與「🔑 授權教學組長為管理員 (自動名單寫入與清除快取)」功能，供管理員靈活操作。
- **`[x]` (2026.07.06-6)**：實作 P1 流程自動化與功能優化：
  - 新增 `scripts/bump-version.ps1` 本地端 PowerShell 一鍵自動升版、安全檢測與 git push 部署腳本。
  - 新增自訂 GitHub Actions 部署工作流與 Python 金鑰替換框架，實行安全零密鑰洩漏。
  - 實作教師「無感靜默更新」機制，在未登入或無動作狀態下自動升級，減少彈窗干擾。
  - 實作「調代課單據一鍵匯出為 Word」功能，可指定月份一鍵下載所有已確認的簽核用 A5 規格單據。
- **`[x]` (2026.07.06-4)**：實作 P0 安全性防護與快取自癒機制，包含自動註銷 Scope 衝突的 Service Worker 以及使用 `sessionStorage` 鎖定防止無限重新整理。
- **`[x]` (2026.07.06-3)**：在登入頁面疊層底部新增授權與原創聲明頁尾 (Footer)，解決登入前無法看到原創連結的問題。
- **`[x]` (2026.07.06-2)**：將原創新北市中和高中教學組詩穎老師的超連結更新為原創開源的起點複製模板連結，避免指向非開源版本，並修復 Service Worker 更新提示與版本同步機制。

---

Made with ❤️ by [阿凱老師](https://www.smes.tyc.edu.tw/modules/tadnews/page.php?ncsn=11&nsn=16#a5)

